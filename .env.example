VITE_SUPABASE_URL=https://xxxxxxxx.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# Opcional: webhook Discord (notificações de ladder + snapshot textual da ordem das listas).
# Criar em Discord → Canal → Integrações → Webhooks. Não commits o URL real.
# Depois de editar o .env, reinicia o dev server (Vite só lê env no arranque).
VITE_DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/1493030610162417806/K9yE11otuSZqrm6LjfPKez0Xjh4iVUoen5-JG6kuXONN0BgQ-JyUSYBwWJ_o7dDK1R3E
# Alternativa: envio via Supabase Edge Function (sem URL no bundle; deploy: supabase functions deploy discord-webhook-proxy)
# No dashboard Supabase → Edge Functions → Secrets: DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/...
# VITE_DISCORD_USE_SUPABASE_EDGE=true

# Screenshot PNG automático da UI: não suportado neste repo (exigiria html2canvas + proxy
# com segredo no servidor). Usa o snapshot em texto nas mensagens acima.